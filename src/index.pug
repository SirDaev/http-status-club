doctype html
html(lang="en")
  head
    title HTTP Response Status Code Tester
  body
    h1 HTTP Response Status Code Tester
    h2 The following codes are currently supported:
    table
      each item in data
        tr
          td
            | #[a(href=item.sourceUrl target="_blank") #{item.code}]
            | #{item.reasonPhrase}